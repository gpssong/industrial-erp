<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.industrial.erp.modules.sales.mapper.SalDeliveryMapper">
    <select id="selectByIdRaw" resultType="com.industrial.erp.modules.sales.entity.SalDelivery">
        SELECT * FROM sal_delivery WHERE id = #{id}
    </select>
</mapper>
