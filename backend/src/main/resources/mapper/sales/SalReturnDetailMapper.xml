<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.industrial.erp.modules.sales.mapper.SalReturnDetailMapper">
    <select id="selectByReturnId" resultType="com.industrial.erp.modules.sales.entity.SalReturnDetail">
        SELECT d.*, p.thickness AS pThickness, p.width AS pWidth, p.density AS pDensity, p.gram_weight AS pGramWeight, p.material AS pMaterial
        FROM sal_return_detail d LEFT JOIN base_product p ON d.product_id = p.id
        WHERE d.return_id = #{returnId} AND d.deleted = 0 ORDER BY d.line_no
    </select>
</mapper>
