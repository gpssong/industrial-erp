<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.industrial.erp.modules.system.mapper.SysMenuMapper">
    <resultMap id="MenuMap" type="com.industrial.erp.modules.system.entity.SysMenu">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="menu_name" property="menuName"/>
        <result column="menu_type" property="menuType"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="perms" property="perms"/>
        <result column="icon" property="icon"/>
        <result column="sort_no" property="sortNo"/>
        <result column="is_visible" property="isVisible"/>
        <result column="status" property="status"/>
    </resultMap>
    <select id="selectMenusByUserId" resultMap="MenuMap">
        SELECT DISTINCT m.*
        FROM sys_user_role ur
        INNER JOIN sys_role_menu rm ON rm.role_id = ur.role_id
        INNER JOIN sys_menu m ON m.id = rm.menu_id AND m.deleted = 0
        WHERE ur.user_id = #{userId} AND m.status = 1
        ORDER BY m.sort_no, m.id
    </select>
    <select id="selectMenusByRoleId" resultMap="MenuMap">
        SELECT m.*
        FROM sys_role_menu rm
        INNER JOIN sys_menu m ON m.id = rm.menu_id AND m.deleted = 0
        WHERE rm.role_id = #{roleId} AND m.status = 1
        ORDER BY m.sort_no, m.id
    </select>
</mapper>
