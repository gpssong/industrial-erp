spring:
  datasource:
    url: jdbc:h2:mem:erp_test;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: false
  data:
    redis:
      host: localhost
      port: 6379
sa-token:
  # P2-10: 测试密钥, 不得在生产环境使用
  jwt-secret-key: ${SA_TOKEN_JWT_SECRET_KEY:industrial-erp-jwt-test-p2-fix}
